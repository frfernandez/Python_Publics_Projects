import Useful_Tools;

print(Useful_Tools.roll_dice(10));

'''
You can use pip program to install Python modules.
It's a package manager. A package manager basically allows you to install, manage, update and uninstall different Python modules.
Use the Command Prompt (Windows) or Terminal (Ubuntu) to use the pip command.
Every new package is installed in the site-packages folder.

pip --version
pip install python-docx
pip uninstall python-docx
'''